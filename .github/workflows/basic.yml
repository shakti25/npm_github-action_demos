name: 'Basic Workflow'
on: workflow_dispatch
jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            -   name: Print Greeting
                run: echo "Hello World!"
            -   name: Print GoodBye
                run: echo "Done - bye!"
            -   name: Add Multi-line commands
                run: |
                    echo "First output"
                    echo "Second output"
